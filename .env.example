# ============================================================================
# Anny — Secrets
# ============================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to version control.
#
# Non-secret settings live in config.yaml (safe to commit).
# Env vars override both .env and config.yaml.
# ============================================================================

# Google Service Account
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=path/to/service-account-key.json

# API Key for REST (X-API-Key) and MCP HTTP (Bearer token) authentication
# Leave empty to disable auth in development
ANNY_API_KEY=

# Sentry DSN for error tracking (optional — leave empty to disable)
SENTRY_DSN=

# Google (can also be set in config.yaml)
GA4_PROPERTY_ID=properties/123456789
SEARCH_CONSOLE_SITE_URL=https://example.com
